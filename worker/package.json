{
  "name": "takeout-fit-to-health-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker backend that parses a Google Fit (Google Takeout) export and converts it into a normalized HealthKit-ingestible payload.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.0"
  }
}
